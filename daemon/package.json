{
  "name": "@cursor-remote/daemon",
  "version": "1.16.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "remote-dev": "./dist/cli/remote-dev.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "remote-dev": "node dist/cli/remote-dev.js"
  },
  "dependencies": {
    "@cursor-remote/protocol": "1.16.0",
    "ws": "8.21.3"
  }
}
